// These shaders should never be used and serve only for testing purposes.
// Fragment shader placeholder.
#version 450 core
layout(location = 0) out vec4 outColor;
void main() {
    outColor = vec4(1.0);
} 
